# 正式任务与阶段计划
依据用户提供的腾讯原始任务书。Owner：@XavierYChen。以本次实际日期记录进展，不补造 8.24 的现场验收。

| 阶段 | 正式要求 | 当前范围 / 下一步 |
| --- | --- | --- |
| 准入 | COCO8 一次 routing snapshot、字段字典、开销方案 | 本次修复与验证目标 |
| P0 | 统一 schema，MoE/MoT/Latent 结构化日志与静态图 | 已有三族 smoke 原型；下一步抽出可复用采集 API、增多图/训练态契约并对接 diagnose_model |
| P1 | TensorBoard/W&B/实时面板，≥3 类训练减速 <10% | 先本地 TensorBoard，选择采样频率，做严格训练 on/off；未验收 |
| P2 | token 路由热图叠加原图，2 分钟 demo，更多族 | 仅对真实空间路由做 overlay，全局路由明确显示全局分布；补 MoA/MoLoRA；未实现 |

## 截止日期
- 9.1–9.7：关键消融、中期演示、PR 草稿、测试文档。
- 9.8–9.12：多 seed/补实验，冻结复现包。
- 9.13–9.14：最终 PR、报告、已知局限和答辩。

## 实施顺序
先锁定基线并验证 smoke，再完成 P0 对外接口；之后三族训练 on/off，再选择面板和采样频率；最后才拓展空间图与更多族。
禁止为可视化修改腾讯核心 forward。适配不了的字段/族用 null、unsupported 表示。

## 风险触发线
字段缺失或非法：失败并保留日志；不能用 0 冒充观测。
GPU 资源不足：明确切 CPU，只报告 CPU 结果。
P1 ≥10%：调整采样并重测，不改变统计口径。
时间不足：交三族 P0、真实失败证据和未完成清单。
